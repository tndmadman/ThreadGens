# Release-baseline top-level pins for the isolated Windows Kokoro environment.
# Keep these exact until a dependency update is validated by timing/audio regressions.
kokoro==0.9.4
soundfile==0.13.1
numpy==2.2.6
