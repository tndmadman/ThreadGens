plugins {
    id 'application'
}

group = 'tndmadman'
version = '0.4.1-runtime-cleanup-image-ready'

repositories {
    mavenCentral()
}

sourceSets.main.java.srcDirs = ['src']

application {
    mainClass = 'redditTxtToImg.CheckedRunner'
}

jar {
    archiveBaseName = 'ThreadGens'
    manifest {
        attributes 'Main-Class': 'redditTxtToImg.CheckedRunner'
    }
}
